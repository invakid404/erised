#! Running
```bash
# Downloading the docker compose config
wget https://raw.githubusercontent.com/invakid404/erised/main/docker-compose.yml

# Use docker-compose to start Erised
docker-compose up
```
